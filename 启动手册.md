# screen-assistance 启动手册（裙带菜 / <本队节点>）

> 队友共享用。密码见 `登录信息表.xlsx`，本文件不写密码。

## 0. 先判断你属于哪种情况

| 情况 | 特征 | 你要做的 |
|---|---|---|
| **A. 只是你电脑重启了** | 大概率是这个 | 只做【第一部分】 |
| **B. 节点上的容器也停了** | 上次收尾时 `docker stop` 过，或隔了很久 | 先做【第二部分】，再做【第一部分】 |

> 你重启电脑**不会**影响节点 —— 节点是远程服务器，容器照常在跑。掉掉的只是你本机的 SSH 隧道。

---

## 第一部分：开隧道（每次必做）

### 1. 打开一个 PowerShell 窗口

```powershell
$env:PYTHONIOENCODING = 'utf-8'      # 防止中文输出乱码，必加
ssh -N -L 18000:127.0.0.1:18000 -p <分配端口> <用户名>@<节点公网入口>
```

输密码后**没有任何输出就是成功**。光标会一直停着 —— **这个窗口全程不许关**，关了 skill 就断。

### 2. 另开一个窗口验证隧道

```powershell
curl http://127.0.0.1:18000/health
```
- 有返回内容 → ✅ 通了
- `Connection refused` → 隧道没建起来，或节点容器没跑（去做第二部分）

### 3. 打开 Cherry Studio，正常对话即可

skill 已安装：`Data\Skills\screen-assistance`，已启用，无需重新安装。

---

## 第二部分：启动节点上的容器

### 1. SSH 登录节点

```bash
ssh -p <分配端口> <用户名>@<节点公网入口>
```

### 2. 查看当前状态

```bash
docker ps -a --format 'table {{.Names}}\t{{.Status}}' | grep screen-assistance
```

### 3. 启动容器（⚠️ 顺序不能反）

```bash
docker start screen-assistance-vlm      # 先：模型服务（加载 2~5 分钟）
docker start screen-assistance-relay    # 后：转发网关（绑 127.0.0.1:18000）
```

**为什么必须先 vlm 后 relay**：relay 启动时要把流量转发到 vlm 容器的内网 IP（`--target 172.18.x.x`），vlm 不在就没有目标。

### 4. 等待就绪（模型加载需要几分钟）

反复执行下面这条，直到输出 `200`：

```bash
curl -s -o /dev/null -w '%{http_code}\n' --max-time 5 http://127.0.0.1:18000/health
```

看到 `200` 才算好了。然后回到【第一部分】开隧道。

---

## 完整自检（一条命令看全貌）

```bash
docker ps --format '{{.Names}}  {{.Status}}' | grep screen-assistance
ss -tlnp 2>/dev/null | grep 18000
curl -s --max-time 5 http://127.0.0.1:18000/health && echo " <- health OK"
```

期望结果：
- 两个容器都是 `Up`
- `LISTEN 127.0.0.1:18000`
- health 有返回

---

## 正常单次调用要多久

**54~76 秒**。配置是 BF16 + 单并发 + eager 模式 + 显存预算 0.45，这是刻意保守的设定，**不是卡死**，别中途 Ctrl+C。

---

## 出问题怎么办

| 现象 | 原因 | 处理 |
|---|---|---|
| `curl` 18000 拒绝连接，但两个容器都 `Up` | relay 的目标 IP 过期（网络被重建过） | 重建 relay：`docker rm -f screen-assistance-relay` 然后 `bash ~/screen-assistance-security-v3/deployment/start-relay.sh` |
| 只启了 vlm，没启 relay | 忘了第二步 | `docker start screen-assistance-relay` |
| 容器 `Exited (137)` | 被强制杀（OOM 或手动） | 重新 `docker start`，注意顺序 |
| 中文输出乱码 | Windows 管道按 GBK 编码 | PowerShell 里先 `$env:PYTHONIOENCODING='utf-8'` |
| 端口 8888 被占用 | 节点自带服务，与本项目无关 | 不用管 |

---

## 收尾（不用时做，做个体面的邻居）

```bash
docker stop screen-assistance-relay screen-assistance-vlm
```
让出 GPU。本机那个隧道窗口 `Ctrl+C` 关掉即可。

---

## 🚫 绝对不要做

- **不要删** `screen-assistance-vlm-before-lock` / `-before-security-v3`（回退用的）
- **不要碰** `~/screen-assistance-security-v3/`（队友正在做的安全加固，迁移未完成）
- **不要** `reboot` / `shutdown` / 改 SSH / 改防火墙 / 改密码
- **不要** `scp` 超过 1 GB 的文件（50 支队共享带宽）
- **不要** 登录别的节点

---

## 架构速查

```
本地 Cherry Studio
  └─ screen-assistance skill
       └─ scripts/invoke_model.py  --provider spark
            │  HTTP → 127.0.0.1:18000
            ▼
      [你电脑] SSH 隧道   ssh -N -L 18000:127.0.0.1:18000 -p <分配端口>
            ▼
      [<本队节点>] screen-assistance-relay   (--network host，纯 TCP 转发)
            ▼  转发到 172.18.x.x:8000
      [<本队节点>] screen-assistance-vlm     (vLLM，隔离内网 --internal)
            └─ Step3-VL-10B BF16 视觉模型
```

**记住一句话：两个容器 + 一条隧道，缺一不可。**
