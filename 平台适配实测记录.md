> 历史记录：本文件保留上一阶段结果。当前状态、后续失败与修订，以《逐项优化与严格复核报告.md》为准。

# 平台适配实测记录

日期：2026-09-25。当前结论：阶跃云端通道与 Spark GPU 环境检查通过；Step3-VL-10B 已在 Spark 成功加载，Skill 配套工具通过 SSH 隧道完成三类虚构中文图片的推理验证。尚未完成真实网站端到端与用户试用。

## 已实际验证

| 项目 | 结果 |
|---|---|
| 阶跃接口 | Step Plan，https://api.stepfun.com/step_plan/v1/chat/completions |
| 云端模型 | step-3.7-flash |
| 最小连接请求 | HTTP 200，正常结束，返回“连接成功” |
| 最小请求耗时 | 0.85 秒，单次观察值，不是性能基准 |
| Skill 配套调用工具 | invoke_model.py 完成一次虚构“已受理，待审核”文字解释，正常返回 |
| 配套调用耗时 | 1.76 秒，单次观察值，不是性能基准 |
| Spark 连接 | 使用分配表中的 SSH 端口及用户成功连接，没有探测其他节点 |
| 硬件 | NVIDIA GB10，ARM64 |
| 系统 | Ubuntu 24.04.3 LTS |
| GPU 驱动 | 580.82.09 |
| 内存 | 系统报告总量 121 GiB，检查时可用约 118 GiB；属于统一内存 |
| 磁盘 | 检查时约 2.7 TB 可用，使用率 24% |
| Docker | 28.3.3 |
| 现有推理镜像 | vllm/vllm-openai:latest，arm64；实际镜像 ID 见下文 |
| 容器内推理环境 | PyTorch 2.11.0+cu130，CUDA 13.0，vLLM 0.22.1，Transformers 5.10.2 |
| GPU 计算检查 | CUDA 可用，GPU 张量求和输出 32.0，临时测试容器退出 |
| 架构注册检查 | vLLM 模型注册表包含 StepVLForConditionalGeneration |
| 现有预置权重 | 手册列出的 /home/xsuper/models 不存在；检查的标准缓存目录没有找到可复用权重 |
| 官方模型源 | Hugging Face 从节点连接失败；阶跃官方 ModelScope 页面对应的 config.json 可读取 |
| 模型下载 | 25 个文件下载完成，5 个权重分片，约 20.36 GB，下载容器退出码 0 |
| 权重结构检查 | 5 个分片的文件边界与索引中的 1066 个张量映射检查通过；不是逐字节源站哈希比对 |
| 本地视觉服务 | Step3-VL-10B BF16，健康检查 HTTP 200，模型名 screen-assistance-vlm |
| 访问限制 | 宿主机仅绑定 127.0.0.1:18000，实测通过 SSH 隧道访问 |
| 权重加载 | 约 140 秒；框架报告模型加载占用 19.17 GiB，不是总内存峰值 |

实测镜像 ID：`sha256:9b2b3cb4d201e48efac830e8b1fd4d5f057be394def6f11cce50f3691d2f8e6a`。标签 latest 可能变化，后续复现应核对这个实际镜像，而不是仅记录标签。

## 已交付的调用能力

`screen-assistance/scripts/invoke_model.py` 支持两个明确分开的通道：

- Spark：图片与任务文字只允许发到本机回环地址，应由 SSH 隧道连接远端。已通过 SSH 隧道对远端模型完成虚构图片推理。
- Step Plan：仅允许文字；需提供云端发送授权标记。密钥由环境变量或终端隐藏输入提供，不保存到文件。固定使用官方 Step Plan 路径，不自动切换到普通 API。

没有自动截图、自动操作网页或自动提交。HTTP 错误、截断输出和空结果均不能当作有效观察。调用工具的七项边界测试通过；Skill 格式校验通过。测试覆盖路径、云端图片拒绝、回环限制、文件类型、输出截断、错误脱敏与拒绝重定向。

## 当前发现与限制

1. 云端样例正确识别“尚未办理完成”，但还推断了“审核通过后的后续环节”。这不由样例页面支持，已收紧提示要求只报告证据；修改后尚未重新进行模型行为验收，不能宣称业务判断测试通过。
2. 已增加模型加载与三类图片推理验证；样例为清晰文字图片，不能据此推出真实复杂网页的准确率。
3. Spark 是主办方远程云节点。截图传过去属于远程传输，不能宣称原始截图始终留在用户电脑。赛事验证仅使用公开或虚构数据。
4. 本次没有保存 API 密钥，也没有把密钥上传到节点。后续调用需要通过环境变量或隐藏输入提供。
5. SSH 首次连接记录了主机公钥，用于后续拒绝不一致的主机公钥；主办方未提供可独立比对的指纹，因此首次公钥未作带外核验。

## 节点上的本任务资源

- 项目目录：`~/screen-assistance-runtime/`
- 权重目录：`~/screen-assistance-runtime/models/Step3-VL-10B/`
- 下载容器：`screen-assistance-model-download`
- 下载源：https://modelscope.cn/models/stepfun-ai/Step3-VL-10B

没有改动 GPU 驱动、重启节点、停止他人容器或开放公网模型端口。下载容器已退出；本轮另行启动模型服务容器 screen-assistance-vlm，当前保持运行。

## 本地视觉实测结果

全部请求经用户电脑的配套 invoke_model.py → SSH 隧道 → Spark 本地 vLLM → Step3-VL-10B 返回，没有调用 Step Plan 或其他云端模型。输入为 evals/fixtures 中人为制作的 1000×620 中文 PNG，不是真实网页截图。模型未收到图片文字的另行转录。

| 样例 | 请求耗时 | 人工核对结论 |
|---|---:|---|
| 已受理，待审核 | 68.468 秒 | 正确识别尚未办好，并引用图片证据 |
| 上传 JPG，页面仅接受 PDF | 29.868 秒 | 找对格式原因，但额外猜测可能超大小，严格评价为部分通过 |
| 已提交申请，应查询进度 | 11.962 秒 | 正确选择进度查询入口 |
| 上传错误复测 | 38.893 秒 | 收紧提示后，正确指出格式不符，并明确大小未知 |

首个请求期间服务记录了一次 Triton 内核即时编译，不能将其耗时当作稳定延迟。4 次请求不是性能基准，不计算或宣称可靠的 P95、真实用户成功率或全面安全率。改动提示后仅复测受影响的上传错误样例，其余样例未重复。

原始响应保存在 evals/results/ 中。脚本仍不执行浏览器动作。风险确认和真实页面核对由宿主与 Skill 工作流负责。

## 运行限制与复现

启动配置见 deployment/start-spark.sh。模型使用 BF16、单并发、8192 上下文与 eager 模式；未进行量化、编译或并发性能优化。模型运行后系统一次抽样显示约 60 GiB 已用、约 60 GiB 可用，这是系统统一内存瞬时值，不是模型独立显存峰值。

运行中存在 tokenizer 正则警告和生成参数忽略警告。当前样例正常返回，但尚未专项验证其对其他输入的影响。实际镜像只有本地 ID，没有已确认的可拉取发行 digest；换机器仍需兼容验证。

模型 config.json SHA256：dd0251b30c25cb18f8985f5e38a40b3cc98cb961ab5f95f390b51e7c4d8a54fa。模型权重来源见上文，尚未固定可重下载的 ModelScope 提交版本。

## 下一阶段验收

接入宿主对真实网页的授权观察与 DOM 核对；扩展到至少 20 个页面和错误样例；进行相同模型有无 Skill 的对照；优化响应时间并进行目标用户试用。上述项目均待执行。

官方参考：[Step Plan 接入说明](https://platform.stepfun.com/docs/zh/step-plan/integrations/reasoning-api)、[阶跃模型说明](https://huggingface.co/stepfun-ai/Step3-VL-10B)、[NVIDIA vLLM 部署指南](https://build.nvidia.com/spark/vllm/instructions)。
