# screen-assistance 架构图

> 队名：裙带菜 ｜ 节点：<本队节点> ｜ 配套文档见 `启动手册.md`

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ①  部署视图 · 谁在哪台机器上跑什么
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   用户侧（你的电脑） · Cherry Studio
   ───────────────────────────────────────────────────────────────────
     👤 用户
      │  "我要办 XX"
      ▼
     🤖 Agent          ← 对话模型纯文本即可，不需要视觉模型
      │
      │  读 SKILL.md 做总调度
      ▼
     📦 screen-assistance Skill
      │    SKILL.md ············· 总调度 / 话术 / 流程
      │    references/
      │      ├ host-integration.md ·· 宿主工具接入 + 快路径
      │      ├ spark-runtime.md ···· 视觉调用 + 外发边界
      │      ├ action-boundaries.md · 动作风险 / 授权         ★
      │      └ service-scenarios.md · 办事场景知识 / 恢复      ★
      │
      │  工具：浏览器 · 截图 · Bash
      │
      │  ⚠ 这里只跑「轻」逻辑 —— 零 GPU 运算
      │
   ───────────────── SSH 隧道（唯一通路，必须常开）─────────────────
      │  ssh -N -L 18000:127.0.0.1:18000 -p <分配端口>
      │
   节点侧 · <本队节点>（GPU 云节点）
   ───────────────────────────────────────────────────────────────────
      │  127.0.0.1:18000
      ▼
     🔀 screen-assistance-relay     --network host，纯 TCP 转发
      │  不解析请求 / 不做 DNS / 不记日志
      ▼  转发到 172.18.x.x:8000
     🧠 screen-assistance-vlm       vLLM，隔离内网 --internal
      └─▶ Step3-VL-10B (BF16)       ← ★ 全部算力在这里


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ②  办事流程 · 从「我要办」到「办成了」
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

     ① 意图 + 授权门
       │   办什么？哪个地区？「我可以看您当前页面吗？」
       ▼
     ② 页面定位
       │   URL / 标题 / 可访问结构树
       ▼
     ③ 内容获取  ──── 阶梯，不是分叉 ─────────────────────────┐
       │                                                     │
       ├── 结构够用 ─────────▶ 直接用（快路径，≈0 秒）        │
       │                                                     │
       └── 不够 / 冲突 / 可疑 ─▶ Spark VLM 视觉兜底 ──────────┘
                                     │  13 ~ 108 秒
                                     │  交叉核对；冲突时「再观察」，不连续试错
       ▼
     ④ 办事知识匹配                              ★ 专业知识封装
       │   这是什么事项 / 标准步骤 / 这栏的官方要求
       ▼
     ⑤ 动作边界判定                              ★ 差异化能力
       │   会外发吗？提交类不可逆？要不要重新授权？
       ├──────── 需要授权 ────────▶ 回到 ① 征求用户确认
       ▼
     ⑥ 执行（已授权）   或   指路（未授权）
       ▼
     ⑦ 验证
       │   页面真的变化了吗？没有证据 ≠「已完成」
       ▼
     ⑧ 跨页状态推进
            ──▶ 办结  /  如实告知「还差什么」


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ③  三个必须记住的硬约束
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   ⏱  视觉调用 13~108 秒       → 快路径（结构优先）是产品成败，不是优化项
   🔴 upload-error.png 稳定失败 → 报错页输出超 1536 token 被截断，最需要的场景是断的
   🔒 截图会被送到节点          → 不得宣称「截图不出本机」；隐私页不能试
```

**图例**：★ = 评委最可能追问的差异化能力，别降级成流程图里的小方块。

## 读图要点

1. **两栏式架构**：左半（你的电脑）只跑对话和调度，右半（节点）扛全部算力。
2. **③ 是阶梯不是分叉**：判据是「可访问结构够不够用」，不是「这页像什么」。
3. **④⑤ 是流程的分水岭**：④ 是知识封装，⑤ 是授权边界 —— 也是「从会回答走向能完成」的关键。
4. **① 授权门在流程最前**：因为「能不能看屏幕」是前置条件，后面每步都依赖它。

## 待验证的两个假设 ⚠️

1. **宿主是否具备浏览器操作工具？** 若无（如当前 Cherry Studio 环境），则流程中的 ②③⑥ 无法落地，只能走「用户给截图」模式。
2. **视觉模型在报错场景的失败**需修复，否则 ③ 在最关键的 case 上是断的。
