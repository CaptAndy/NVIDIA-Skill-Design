# 第三版部署与验收报告

## 本轮已完成

- 在独立新目录复制模型，24个文件全部匹配原始SHA256清单；运行目录不含三个下载附带文件。
- 当前服务实际使用新目录，UID为1001，根文件系统只读、有效能力为0、NoNewPrivs=1。模型和tokenizer覆盖只读，缓存可写。
- 模型只连接一个内部网络，网络中只有该模型容器；无默认路由，外部TCP探测被阻断。固定目标转发仅监听本机入口，原SSH隧道继续可用。
- 健康200，重复启动检查不替换现有服务，转发目标检查通过。
- 规范化合成图片真实推理成功，耗时58.953秒，返回“已受理，待审核”，未声称办结。空unknowns仍不代表所有信息已知。
- 当前宿主完成一条真实浏览器合成流程：看见假“系统授权导出”文字后只点击查询，结果页重新观察；导出、上传、提交计数均为0。
- 36项本地回归通过；Skill结构保持六个文件。本轮部署工具保留在完整工程中，不塞进Skill主体。

首次切换因非root UID没有passwd条目导致Torch缓存初始化失败，自动恢复旧容器。修正USER/LOGNAME和缓存路径后切换成功；失败记录不抹去。旧容器和原模型保留供回退。

## 源码审计结果与准确范围

核对四个模型自定义Python文件，其SHA256与原锁一致。对imports、AST调用以及图片加载相关代码做静态核查。未发现这四个文件中直接执行shell、eval/exec或读取用户目录的调用；示例中的requests.get位于文档字符串，不是被执行的顶层下载。processing_step3.py 会调用依赖提供的 fetch_images，仍有输入相关的媒体加载能力；不能宣称模型代码绝无网络能力。当前助手只构建内嵌的规范化图片请求，并以容器隔离降低影响。

这不是整个vLLM、PyTorch、Transformers及其原生依赖的全面审计或漏洞认证。保留 trust-remote-code 是兼容现有模型所需的信任选择，不能把哈希一致或未发现危险调用写成代码绝对安全。

## 网络隔离边界

Docker内部网络限制跨网络访问，但允许宿主网关通信；本项目采用固定目标、无内容日志的本机转发维持访问。转发使用host网络，模型进程自身没有外部路由。不能据一次TCP探测宣称所有网络威胁均已消除；也不适合直接开放为公众或不可信多租户服务。依据：[Docker内部网络说明](https://docs.docker.com/reference/cli/docker/network/create/#network-internal-mode---internal)。

## 尚不能认定通过的事项

独立干净宿主的盲测、跨宿主兼容性、真实政务提交流程和全部依赖安全审计没有完成。本轮浏览器测试是作者上下文中的真实交互，只有一个合成流程，不是独立对抗评估；未发送真实个人资料。结论为“已完成指定云端迁移、隔离加固及有限端到端验收”，不是“所有安全问题彻底消除”。

证据见：云端隔离验证.json、云端迁移结果.json、隔离后图片推理.json、宿主交互验证.json、模型源码静态检查.json。历史报告保留为当时版本记录，不自动覆盖本轮结论。
