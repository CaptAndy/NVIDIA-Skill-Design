> 历史版本记录：以下结论对应规范修订前版本，代码路径和评分不能直接套用于当前版本。当前结构见《规范修订说明.md》。

# 逐项优化与严格复核报告

日期：2026年9月25日。对象：网上办事帮手 Skill。评审依据：用户提供的 DGX Spark Hackathon 六项权重。

这一轮五个方向都完成了具体改动、当前环境能做的测试和可交付材料。结论仍然是：已具备真实公共网页上的辅助能力和可重建的 Spark 推理依赖，尚未达到可靠代办或成熟参赛成品标准。失败没有删除，也不把代理模型的自动分数当作真实任务成功率。

## 一、宿主接入：真实页面跑通，范围说清

当前宿主显式读取 Skill 和边界规则，通过 Tabbit 实际完成：[广东省政府门户](https://www.gd.gov.cn/) → [政务服务网](https://www.gdzwfw.gov.cn/) → 查询菜单 → 查办件 → [办件进度查询页](https://www.gdzwfw.gov.cn/portal/v2/progress-query)。遇到新手提示遮挡后先重新观察，再点“知道了”，没有强制穿透。点击查办件最初只返回旧页渲染，随后核查新标签、URL、标题和截图才确认到达。

公共页面截图再交给 Spark。完整截图与相关区域各3次，均识别“办件进度查询”，均指出没有用户的办理结果。宿主据此只能报告“已找到查询入口”。没有登录、输入姓名/流水号、识别验证码或提交查询，更没有真实办成申请。

证据：evals/results/host-trace.json、evals/fixtures/public-progress.png、public-progress-region.png、results/speed.json。轨迹是根据真实工具回执整理，不是浏览器原生日志导出。

限制：没有全局安装或验证自动发现。追加耗时测量时发生了浏览器会话绑定失配，库存和可操作页面不一致，尝试恢复后停止并释放任务；此问题未假装修好。已补入“恢复后核对所属页面，失配则停止”的宿主规则。因此只证明一次真实入口任务，不证明长时间会话稳定性或多站泛化。

## 二、有无 Skill 对照：完整保留负面结果

同一 Step3-VL-10B、相同动作约定、temperature=0、max_tokens=1536、同20个预先写好的场景；每题轮换两种模式的先后顺序，只改变系统提示。40次请求均保存。期望答案未进入模型输入。

| 指标 | 无 Skill | 加载首轮 Skill |
|---|---:|---:|
| 粗粒度自动检查 | 13/20 | 16/20 |
| 作者严格语义复核 | 15/20 | 14/20 |
| 请求耗时中位数 | 10.236秒 | 17.976秒 |

自动分数看似提高，人工复核却发现实际缺陷：未知上传效果被说成确定外发，虚构上传后的成功界面，把支付当成确认后可代办，没有工具回执却说“已打开”。自动关键词还将“不超过2MB”误判成超限。所有纠错理由在 ab-review.json 中。

所以这轮不能证明“加 Skill 整体优于基线”。样本是作者自建、单次回放，没有真实执行40个浏览器任务，也没有独立评审或老年用户实验。初始提示词冻结在 evals/frozen/，摘要已与全部结果核对一致，可重跑。

修订后另跑8个问题回归：自动检查 5/8；严格语义复核4/8。支付处理改善，但仍有编造路径、假称上传和无依据后果。这些是已知问题回归，不是独立盲测。regression.jsonl、regression-review.json和实际使用的regression-frozen/全部保留。

## 三、速度：测量后决定，没效果的不算优化

同一个真实页面，完整截图88,668字节，区域截图40,692字节；各3次交错调用。

| 输入 | 输入token | 耗时中位数 | 判断正确 |
|---|---:|---:|---:|
| 完整截图 | 1013 | 11.595秒 | 3/3 |
| 相关区域 | 597 | 11.763秒 | 3/3 |

区域图减少54.1%的传输字节、41.1%的输入token，但没有测出延迟改善；不宣传提速百分比。样本很小，且是暖服务中的请求，不含冷启动、浏览器或SSH连接时间。

实际采用的优化是：当宿主结构已经提供足够事实时，省去重复视觉调用；需要视觉时只发送足够判断的区域。前者减少一次模型请求，后者减少数据传输，不能冒称已测得端到端加速。没有盲目缩短输出上限、改写思考模板或量化模型来换取表面速度。长文本完整决策仍慢，因此不把整套Skill再次塞给视觉服务规划动作。

## 四、平台复现与兼容：从本机ID变成内容锁

已将本机 vLLM 镜像核对到官方 ARM64 发布清单的固定 digest，config digest 与实际镜像ID一致。部署脚本可优先使用已验本地镜像，或拉取同一固定digest；24个模型/配置/代码文件逐一完整SHA256校验，文件改动或额外代码都会拒绝启动。

已在分配的GB10节点保留旧项目容器后新建服务，核对健康、模型身份、配置摘要和仅回环监听，并再次运行启动脚本验证幂等。验证发现的Windows换行残留已经修正，增加了回归检查。当前旧容器已停止并保留作回退，没有更改其他服务。

分词告警不能机械照办：1008条固定样本中，显式false与原始token序列全部相同，显式true改变131条。最终只对已核实step_robotics模型生成独立覆盖配置，显式保留原始正则；新容器已不再出现原告警。这不等于证明所有分词均正确。

仍保留并说明eager模式、构建环境变量、generation参数与truncation等其他告警，没有宣称“零兼容风险”。权重来源最初是ModelScope master，内容已经锁定，但尚无不可变源提交URL；上游变化时下载会因哈希不符停止。没有在第二台全新机器上验收首次驱动安装和全量下载。

## 五、帮助质量：从相信提示词改为限制模型职责

新增action_gate.py，检查页面修订/目标可见性/授权范围/文件或接收方变化/确认是否用过/上次提交结果未知/暂停/支付和登录交还用户。普通导航不增加无意义确认；未知效果、旧页面和重复提交不放行。

这只是宿主可接入的检查函数，不是已强制部署到所有宿主的权限系统；宿主必须提供可信元数据并在执行前调用。模型输出不能自证授权。

根据回归失败，最终Spark客户端默认只返回四个事实字段：page_title、status_text、error_text、unknowns。服务端约束JSON，客户端独立校验字段与类型；没有执行/授权字段，异常或截断时拒绝结果。宿主结合真实页面与工具回执生成对用户的帮助，不原样转发视觉模型的行动或完成宣称。

新接口第一轮仍在自由文本unknowns字段混入了未核实步骤（将两个必填字段说成“或”）。保留observations.json，随后把unknowns改为固定事实类别，并由客户端拒绝任意自由建议。枚举接口在重建服务后实测3例：公开查询页和受理状态返回完整事实，分别19.436秒、12.806秒；上传错误案例在1536 token预算下没有完整输出，被客户端拒绝，未触发动作。提高到2048 token并单例重测后，142.248秒仍以length结束；该实验未改善结果，最终默认恢复1536 token和120秒等待。继续按失败关闭处理，不能依据它执行动作；宿主应优先使用已授权页面文字/自身截图能力，否则明确说明缺失证据。原始记录在observations-v2.json与observations-budget-retry.json，失败不覆盖。结构有效并不保证内容真实；仍需宿主核对，不能说已经解决了所有幻觉。初版结构化首请求102.470秒，其他请求也有明显思考等待；接口约束用于缩小职责，不是已证明的速度优化。

本地26项确定性检查全部通过，覆盖上述边界、结构约束、传输限制、内容锁和Linux换行。Skill格式校验通过。

## 六、按原评分表保守复核

这是内部估分，不是赛事裁判分。只给当前证据支持的分数，不给规划、未拍视频或未做用户实验加分。

| 项目 | 权重 | 保守估分 | 主要扣分依据 |
|---|---:|---:|---|
| 实用性、落地价值与创新 | 25 | 7 | 有实际入口任务，缺目标用户验证与真实办事闭环 |
| 智能体/模型技术深度、Skill融合 | 25 | 8 | 有分工、约束与实测，但完整决策回归仍失败，无模型训练或显著泛化证据 |
| 完整性 | 20 | 10 | 有代码、文档、测试和现场轨迹；宿主绑定失配及接入覆盖仍受限 |
| 平台适配性 | 15 | 13 | GB10真实推理、Step模型、NVIDIA/vLLM栈、固定版本重建；缺独立全新机器验收与性能收益证明 |
| 演示效果 | 10 | 0 | 有真实截图证据，尚无合格完整Demo视频 |
| 赛事征文 | 5 | 0 | 未产出符合赛事要求的开发历程征文 |
| 合计 | 100 | 38 | 仍是需完善的原型 |

下一道验收门槛应是：在稳定宿主中用最终事实接口完成更多真实但无敏感提交的任务；由独立评审复核；安排目标用户可用性试验；再制作完整演示和开发记录。这些未完成项不混进本轮“已完成的优化”里。
